apiVersion: 1

contactPoints:
  - orgId: 1
    name: email-ops
    receivers:
      - uid: email-ops-uid
        type: email
        settings:
          addresses: "egarabini@gmail.com"

  # Slack: descomente abaixo e substitua a URL pelo webhook real
  # Para obter o webhook: Slack → Apps → Incoming Webhooks → Add New
  # - orgId: 1
  #   name: slack-ops
  #   receivers:
  #     - uid: slack-ops-uid
  #       type: slack
  #       settings:
  #         url: "https://hooks.slack.com/services/REAL/WEBHOOK/URL"
  #         channel: "#intellicare-alertas"
  #         title: "IntelliCare Alert: {{ .CommonLabels.alertname }}"
  #         text: "{{ .CommonAnnotations.description }}"

