apiVersion: 1

policies:
  - orgId: 1
    receiver: email-ops
    group_by: [alertname, severity]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    # Slack: descomente abaixo quando slack-ops estiver configurado em contact-points.yaml
    # routes:
    #   - receiver: slack-ops
    #     matchers:
    #       - severity = critical
    #     continue: true

