# 2026-02-24 14:35 - Execução da 2.1.C (Comunicação Entre Módulos)

## Escopo executado

1. Revalidar estabilidade do stack completo (`docker compose ps`).
2. Executar discovery e roteamento da WANDA.
3. Validar fluxos cross-módulo por chamadas reais.
4. Registrar gaps de integração não implementados.

## Ajuste necessário antes dos testes

Foi identificado que a WANDA estava configurada com URLs internas incorretas no `docker-compose.full.yml`:

- Antes: `http://<servico>:8001/8002/...` (portas de host)
- Correto na rede Docker: `http://<servico>:8000`

Correção aplicada no serviço `wanda`:

- `INTELLICARE_OSWALDO_URL=http://oswaldo:8000`
- `INTELLICARE_FLORENCE_URL=http://florence:8000`
- `INTELLICARE_ZILDA_URL=http://zilda:8000`
- `INTELLICARE_DONABEDIAN_URL=http://donabedian:8000`
- `INTELLICARE_COMUNICACAO_URL=http://comunicacao:8000`
- `INTELLICARE_GERALDA_URL=http://geralda:8000`

Após ajuste, container `wanda` foi recriado.

## Evidências geradas

- `20260224-1431_EXECUCAO_COMUNICACAO.json`
- `20260224-1434_CHECKS_COMPLEMENTARES.json`

## Resultado dos fluxos (cross-módulo)

### Fluxo 1: WANDA -> Oswaldo

- Rota: `POST /api/v1/route` com mensagem clínica de DRC/diabetes/hipertensão.
- Resultado: `target_modules=["intellicare-oswaldo"]`.
- Execução: `POST /api/v1/chat`.
- Evidência: `modules_used=["intellicare-oswaldo"]`.

### Fluxo 2: WANDA -> Florence

- Rota: `POST /api/v1/route` com mensagem de interpretação laboratorial.
- Resultado: `target_modules=["intellicare-florence"]`.
- Execução: `POST /api/v1/chat`.
- Evidência: `modules_used=["intellicare-florence"]`.

### Fluxo 3: WANDA -> Zilda

- Rota: `POST /api/v1/route` com mensagem de CNES/UBS.
- Resultado: `target_modules=["intellicare-zilda"]`.
- Execução: `POST /api/v1/chat`.
- Evidência: `modules_used=["intellicare-zilda"]`.

## Verificações complementares

- `portal` respondeu HTTP 200 em `http://localhost:3001`.
- `wanda` respondeu healthy com `modules_online=5`.
- `donabedian`:
  - `GET /api/v1/health` -> 200
  - `GET /api/v1/info` -> 500 (impacta discovery da WANDA)
- `GET /api/v1/fhir-proxy` na WANDA -> 404 (endpoint não existe atualmente).
- Criação de plano na Geralda (`POST /api/v1/plans`) funcionou, mas não houve sincronização automática em Grahame (fluxo ainda não implementado).

## Conclusão da 2.1.C

- Critério mínimo de aceite atendido: **3 fluxos cross-módulo validados**.
- A fase foi executada e registrada.
- Permanecem gaps de produto/integração para próximas fases (WANDA<->Grahame e Geralda->Grahame).
