# Sprint — 2026-04-04

> Planejador: ARQUITETO
> Base: fechamento sprint 2026-03-28 + roadmap Clinical Squad
> Devs disponíveis: DEV-1, DEV-2, CODEX, DEV-3/4

---

## Metas da Sprint

1. **Clinical Squad em produção** — Florence IA + Oswaldo base no ClinicoUI
2. **Portal do paciente completo** — jornadas e histórico clínico acessíveis
3. **Staging sincronizado** — todas as DEMs da sprint aplicadas e validadas no VPS

---

## Backlog Priorizado

### P0 — Staging Full Sync (DEV-3/4 · DEM-060 · ~1.5h)

Pull das DEMs 054–059, rebuild, migrations 012–014, smoke nos novos endpoints.
Executar após as entregas de P1/P2/P3 estarem em main.

Spec: `docs/demandas/DEM-060_STAGING_FULL_SYNC/BRIEFING.md`

---

### P1 — Florence IA (DEV-2 · DEM-057 · ~4h)

Endpoint `POST /florence/notes/suggest` com fallback rule-based.
Botão "Sugerir SOAP com IA" no `FlorenceNoteEditor` — Hybrid, nunca auto-salva.
Variáveis `FLORENCE_LLM_URL/API_KEY/MODEL` opcionais.

Spec: `docs/demandas/DEM-057_FLORENCE_IA/BRIEFING.md`

---

### P2 — Oswaldo Módulo Base (DEV-1 · DEM-058 · ~4h)

Migration 014 `prescriptions`, busca CID-10 textual, CRUD de prescrições.
Aba "Prescrição" em `EncounterView` com `OswaldoCID10Search` + `OswaldoPrescriptionEditor`.

Spec: `docs/demandas/DEM-058_OSWALDO_MODULO_BASE/BRIEFING.md`

---

### P3 — Portal Paciente Notas (CODEX · DEM-059 · ~3h)

`GET /portal/me/journeys` + `GET /portal/me/clinical-notes` (sem `soap_a`).
Páginas "Minhas Jornadas" e "Histórico Clínico" no PacienteUI.
Privacidade: avaliação diagnóstica omitida do resumo do paciente.

Spec: `docs/demandas/DEM-059_PORTAL_PACIENTE_NOTAS/BRIEFING.md`

---

## Distribuição

```
DEV-3/4 → P0  DEM-060 Staging Full Sync  (~1.5h) — após main ter P1+P2+P3
DEV-2   → P1  DEM-057 Florence IA         (~4h)   — suggest SOAP + botão Hybrid
DEV-1   → P2  DEM-058 Oswaldo Módulo Base (~4h)   — prescrições + CID-10 + EncounterView
CODEX   → P3  DEM-059 Portal Paciente     (~3h)   — jornadas + histórico no PacienteUI
```

---

## Riscos

| Risco | Probabilidade | Mitigação |
|-------|--------------|-----------|
| `FLORENCE_LLM_URL` não configurada no staging | Alta | Fallback rule-based implementado — não bloqueia entrega |
| Migration 014 conflito com schema de `encounters` | Baixa | DEV-1 verifica FK antes de criar migration |
| Portal expõe `soap_a` acidentalmente | Baixa | Teste explícito `test_my_clinical_notes_no_soap_a` cobre o caso |
| WhatsApp desconectar entre DEMs | Baixa | DEV-3/4 verifica `connectionState` no STEP-004 do DEM-060 |

---

## Critérios de Aceite da Sprint

| Item | Critério |
|------|----------|
| P1 Florence IA | `POST /florence/notes/suggest` retorna SOAP; fallback sem LLM; botão Hybrid no ClinicoUI |
| P2 Oswaldo | `POST /oswaldo/prescriptions` cria prescrição; busca CID-10 funciona; aba no EncounterView |
| P3 Portal | `/me/journeys` e `/me/clinical-notes` retornam 200; `soap_a` ausente no resumo |
| P0 Staging | Migrations 012–014 aplicadas; 3 novos endpoints respondendo 200 no VPS |

---

## Próximo ciclo (pós-sprint)

- **DEM-061** Oswaldo IA — sugestão de CID-10 e posologia via LLM (mesmo padrão do DEM-057)
- **DEM-062** Relatório PDF Clínico — exportar encontro completo (notas Florence + prescrição Oswaldo) como PDF via WeasyPrint
- **DEM-063** Testes E2E Clinical Squad — cobertura Playwright para Florence + Oswaldo + Portal notas
