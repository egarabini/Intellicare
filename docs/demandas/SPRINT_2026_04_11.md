# Sprint — 2026-04-11

> Planejador: ARQUITETO
> Base: fechamento sprint 2026-04-04 + Clinical Squad completo
> Devs disponíveis: DEV-1, DEV-2, CODEX, DEV-3/4

---

## Metas da Sprint

1. **IA no Clinical Squad** — Oswaldo com sugestão de CID-10 e posologia via LLM
2. **PDF clínico completo** — relatório de encontro com Florence + Oswaldo exportável
3. **Cobertura E2E** — pytest + Playwright cobrindo todos os fluxos do Clinical Squad
4. **Staging validado** — Clinical Squad funcionando no VPS

---

## Backlog Priorizado

### P0 — Staging Clinical Squad (DEV-3/4 · DEM-064 · ~1.5h)

Sync pós DEMs 061–063. Smoke Florence IA, Oswaldo IA, PDF clínico.
Executar após P1+P2+P3 em main.

Spec: `docs/demandas/DEM-064_STAGING_CLINICAL_SQUAD/BRIEFING.md`

---

### P1 — Oswaldo IA (DEV-2 · DEM-061 · ~3.5h)

`POST /oswaldo/suggest` retorna CID-10 + itens de prescrição.
Mesmo padrão LLM de DEM-057 Florence IA — `_call_llm()` + fallback rule-based.
Botão "Sugerir com IA" no `OswaldoPrescriptionEditor`.

Spec: `docs/demandas/DEM-061_OSWALDO_IA/BRIEFING.md`

---

### P2 — PDF Clínico Completo (DEV-1 · DEM-062 · ~3.5h)

`GET /encontros/{id}/report.pdf` — WeasyPrint + Jinja2.
Template com notas Florence (SOAP ou livre) + prescrições Oswaldo.
Botão `IconFileTypePdf` no `EncounterView`.

Spec: `docs/demandas/DEM-062_PDF_CLINICO_COMPLETO/BRIEFING.md`

---

### P3 — E2E Clinical Squad (CODEX · DEM-063 · ~3h)

8 pytest de integração (Florence, Oswaldo, Portal).
5 specs Playwright (florence, oswaldo, paciente portal).
Zero regressão nos testes anteriores.

Spec: `docs/demandas/DEM-063_E2E_CLINICAL_SQUAD/BRIEFING.md`

---

## Distribuição

```
DEV-2   → P1  DEM-061 Oswaldo IA           (~3.5h) — suggest CID-10 + prescrição, Hybrid
DEV-1   → P2  DEM-062 PDF Clínico          (~3.5h) — WeasyPrint Florence+Oswaldo, EncounterView
CODEX   → P3  DEM-063 E2E Clinical Squad   (~3h)   — 8 pytest + 5 Playwright
DEV-3/4 → P0  DEM-064 Staging Validation  (~1.5h)  — após P1+P2+P3 em main
```

---

## Riscos

| Risco | Probabilidade | Mitigação |
|-------|--------------|-----------|
| `tenant_session(ctx)` em `get_encounter_full()` — mesmo gotcha de DEM-058 | Média | BRIEFING já referencia o padrão correto — DEV-1 consultar `modules/florence/repository.py` |
| Playwright não encontra `data-testid` nos novos componentes | Média | CODEX verifica atributos antes de escrever spec — ou usa seletores CSS/texto |
| Oswaldo IA duplica `_call_llm()` de Florence | Baixa | BRIEFING sugere extrair para `modules/shared/llm.py` se DEV-2 quiser evitar duplicação |
| Evolution desconectar no staging antes de DEM-064 | Baixa | DEV-3/4 verifica `connectionState` no STEP-003 |

---

## Critérios de Aceite da Sprint

| Item | Critério |
|------|----------|
| P1 Oswaldo IA | `POST /oswaldo/suggest` retorna CID-10 + itens; fallback funciona; 2 testes |
| P2 PDF Clínico | PDF com notas + prescrições gerado; botão no EncounterView; bytes `%PDF` |
| P3 E2E | 8 pytest + 5 Playwright passando; contagem total não cai |
| P0 Staging | Smoke 4 endpoints OK; Evolution `open`; PDF gerado no VPS |

---

## Próximo ciclo (pós-sprint)

O Clinical Squad estará completo (Florence + Oswaldo + Portal + E2E + PDF).
Próximas direções possíveis:
- **DEM-065** Multi-tenant avançado — isolamento de dados entre tenants, relatório cross-tenant para admin
- **DEM-066** Notificações push PacienteUI — PWA com service worker, push quando jornada atualiza status
- **DEM-067** Kestra avançado — flows com condicionais por canal + retry por timeout
