# ============================================================================
# INTELLICARE-FLORENCE — Hardened Multi-Stage Dockerfile
# ============================================================================

# ----------------------------------------------------------------------------
# BUILD STAGE — Runs as root, has build tools
# ----------------------------------------------------------------------------
FROM python:3.13-slim AS builder

WORKDIR /build

# Install build dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    g++ \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Install intellicare-core to /root/.local
COPY ./intellicare-core /intellicare-core
RUN pip install --no-cache-dir --user -e /intellicare-core

# Install intellicare-auth (optional)
COPY ./intellicare-auth /intellicare-auth
RUN pip install --no-cache-dir --user -e /intellicare-auth || true

# Install florence dependencies
COPY ./intellicare-florence/pyproject.toml ./intellicare-florence/README.md* /build/
RUN pip install --no-cache-dir --user . || true

# Copy florence source and install to /root/.local
COPY ./intellicare-florence /build/intellicare-florence
RUN pip install --no-cache-dir --user -e /build/intellicare-florence

# ----------------------------------------------------------------------------
# RUNTIME STAGE — Distroless, runs as nobody
# ----------------------------------------------------------------------------
FROM gcr.io/distroless/python3-debian12 AS runtime

# Copy Python packages from builder
COPY --from=builder /root/.local /root/.local

# Copy app
COPY --from=builder /build/intellicare-florence /app/intellicare-florence

# Ensure Python finds user-installed packages
ENV PATH=/root/.local/bin:$PATH
ENV PYTHONPATH=/root/.local/lib/python3.13/site-packages:$PYTHONPATH

# Run as non-root user
USER nobody

# API Target (porta 8000)
FROM runtime AS api
EXPOSE 8000
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD wget --no-verbose --tries=1 --spider http://localhost:8000/api/v1/health || exit 1
CMD ["python3", "-m", "uvicorn", "florence.api.app:create_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]

# UI Target (porta 8502) - Streamlit precisa de shell
FROM python:3.13-slim AS ui
WORKDIR /app
COPY --from=builder /root/.local /root/.local
COPY --from=builder /build/intellicare-florence /app/intellicare-florence
ENV PATH=/root/.local/bin:$PATH
ENV PYTHONPATH=/root/.local/lib/python3.13/site-packages:$PYTHONPATH
EXPOSE 8502
HEALTHCHECK --interval=30s --timeout=5s --start-period=30s --retries=3 \
    CMD curl -f http://localhost:8502/_stcore/health || exit 1
CMD ["streamlit", "run", "florence/ui/main.py", "--server.port=8502", "--server.headless=true"]
