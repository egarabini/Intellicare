version: "3.9"

services:
  intellicare-pierre:
    build: .
    container_name: intellicare-pierre
    ports:
      - "8009:8009"
    environment:
      - TAVILY_API_KEY=${TAVILY_API_KEY:-}
      - NCBI_API_KEY=${NCBI_API_KEY:-}
      - OLLAMA_URL=${OLLAMA_URL:-http://host.docker.internal:11434}
      - PIERRE_LLM_MODEL=${PIERRE_LLM_MODEL:-qwen2.5:72b}
      - PIERRE_LLM_MODEL_FALLBACK=${PIERRE_LLM_MODEL_FALLBACK:-qwen2.5:7b}
      - REDIS_URL=redis://redis:6379/1
      - PIERRE_PORT=8009
      - PIERRE_TAVILY_RATE_LIMIT_PER_HOUR=${PIERRE_TAVILY_RATE_LIMIT_PER_HOUR:-100}
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8009/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    networks:
      - intellicare-network

  redis:
    image: redis:7-alpine
    container_name: pierre-redis
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    networks:
      - intellicare-network

networks:
  intellicare-network:
    name: intellicare-network
