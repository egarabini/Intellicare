# 2026-02-24 20:38 - Plano de Inicio da FASE 3.1

## Premissas

1. MINERVA expoe SSE MCP endpoint estavel.
2. PIERRE expoe SSE MCP endpoint estavel.
3. WANDA possui configuracao de URL por ambiente (`minerva_mcp_url`, `pierre_mcp_url`).

## Backlog tecnico imediato

1. Criar `wanda/mcp/base_client.py` com `connect`, `disconnect`, `list_tools`, `call_tool`.
2. Criar adaptadores:
   - `wanda/mcp/minerva_client.py`
   - `wanda/mcp/pierre_client.py`
3. Criar wrappers de tools para LangGraph em `wanda/tools/mcp_tools.py`.
4. Injetar clients no lifecycle do app (`startup/shutdown`).
5. Cobrir com testes unitarios mockando SSE MCP.

## Criterio de pronto minimo

1. Query cientifica no WANDA invoca PIERRE com fallback quando indisponivel.
2. Documento enviado no WANDA invoca MINERVA com fallback quando indisponivel.
3. 15+ testes de MCP executando sem dependencias externas reais.

## Dependencias externas a confirmar

1. Contrato JSON das tools MCP de MINERVA.
2. Contrato JSON das tools MCP de PIERRE.
3. Timeouts/politica de retry esperada por produto.
