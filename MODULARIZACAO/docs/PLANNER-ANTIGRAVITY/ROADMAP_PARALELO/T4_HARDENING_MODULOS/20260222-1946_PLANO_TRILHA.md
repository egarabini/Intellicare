# T4 — Hardening de Módulos

**Status:** 🔴 Urgente  
**Dependências:** Nenhuma  
**Contexto:** Containers rodando mas "unhealthy"; imports de auth falhando em módulos sem Keycloak configurado

## Fases

| Fase | Descrição | Esforço | Impacto |
|------|-----------|---------|---------|
| **F1** | Padronizar auth opcional | 2-3 dias | Alto (todos os módulos) |
| **F2** | Deps opcionais no Docker | 1-2 dias | Médio (Dockerfiles) |
| **F3** | Testes de integração staging | 2-3 dias | Baixo (sem mudança de código) |

---

### F1 — Auth Opcional Padronizado
> Donabedian já tem o padrão. Replicar para todos os módulos.

Padrão a aplicar (já validado em Donabedian):
```python
try:
    from intellicare_auth import get_current_user, requires_role
    AUTH_AVAILABLE = True
except ImportError:
    AUTH_AVAILABLE = False
    async def get_current_user():
        return {"sub": "anonymous", "preferred_username": "anonymous"}
    def requires_role(role: str):
        def decorator(func): return func
        return decorator
```

Módulos que precisam deste padrão:
- [ ] intellicare-oswaldo
- [ ] intellicare-florence
- [ ] intellicare-zilda
- [ ] intellicare-geralda
- [ ] intellicare-grahame
- [ ] intellicare-nise (orquestrador)
- [ ] intellicare-comunicacao

### F2 — Dockerfiles + Deps
- [ ] Garantir que todos os Dockerfiles instalam `intellicare-core`
- [ ] `intellicare-auth` como dep opcional (COPY + pip install em try)
- [ ] Padronizar `psycopg[binary]` em todos os `pyproject.toml`
- [ ] Rebuild sem cache de todas as imagens
- [ ] Validar todos os containers "Up"

### F3 — Testes de Integração
- [ ] Script de smoke test: `curl` em `/health` de cada módulo
- [ ] Testar endpoints principais de cada módulo em staging
- [ ] Documentar resultados em relatório
