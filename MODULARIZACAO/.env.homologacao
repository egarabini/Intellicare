# ============================================================================
# IntelliCare - Servidor de STAGING (antigo HOMOLOGAÇÃO)
# ============================================================================
# ⚠️  AVISO DE SEGURANÇA - FAVOR LER ⚠️
# ============================================================================
# Este arquivo foi RENOMEADO de .env.homologacao para .env.staging
# como parte do padrão V2 de entrega.
#
# As credenciais originais foram COMPROMETIDAS e ROTACIONADAS.
# NÃO use as senhas antigas deste arquivo.
#
# Use .env.staging como referência canônica para staging.
#
# Histórico:
# - 2026-02-27: Credenciais expostas no Git (incidente de segurança)
# - 2026-02-27: Iniciada rotação de todas as credenciais
# - 2026-02-27: Arquivo substituído por placeholders
#
# Para configuração do servidor, use:
#   cp .env.staging .env
#   # Edite .env com as NOVAS credenciais rotacionadas
# ============================================================================
# Este arquivo é mantido apenas por compatibilidade temporária.
# Será REMOVIDO em breve. Use .env.staging.
# ============================================================================

# Ver .env.staging para configuração canônica
# Todas as senhas devem ser CHANGE_ME_* ou valores rotacionados

ENVIRONMENT=staging
SERVER_IP=167.86.97.142

# ⚠️ CREDENCIAIS EXPOSTAS - NÃO USE ESTES VALORES ⚠️
# Use .env.staging e configure novas senhas fortes
POSTGRES_PASSWORD=ROTATED_USE_NEW_PASSWORD
REDIS_PASSWORD=ROTATED_USE_NEW_PASSWORD
GRAFANA_ADMIN_PASSWORD=ROTATED_USE_NEW_PASSWORD
ROCKETCHAT_ADMIN_PASSWORD=ROTATED_USE_NEW_PASSWORD
ROCKETCHAT_BOT_PASSWORD=ROTATED_USE_NEW_PASSWORD
JITSI_APP_SECRET=ROTATED_USE_NEW_SECRET
FLOWISE_PASSWORD=ROTATED_USE_NEW_PASSWORD
KEYCLOAK_CLIENT_SECRET=ROTATED_USE_NEW_SECRET

# ============================================================================
# IMPORTANTE: Leia FASE4_INCIDENTE_SEGURANCA_CREDENCIAIS.md
# ============================================================================
